import { colors, tabBar } from './colors';
import { radius, spacing } from './spacing';
import { fontSize, fontWeight } from './typography';

export const theme = {
  colors,
  tabBar,
  spacing,
  radius,
  fontSize,
  fontWeight,
};

export { colors, tabBar, spacing, radius, fontSize, fontWeight };
